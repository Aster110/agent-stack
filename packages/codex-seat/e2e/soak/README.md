# Legacy seat soak harness

`soak-ping.ts` and `soak-report.ts` exercise the legacy codex-seat CLI. They do not establish unified brain/WeChat acceptance. Use a dedicated test seat and state directory; never run this harness against another user's live seat.

The public release omits the author's scheduled-job template and captured evidence. Unified stable-release requirements are in `../../../../docs/RELEASE-POLICY.md`.
